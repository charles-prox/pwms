import React from "react";

const BoxDetailsGridView = () => {
    return (
        <div className="border p-4 rounded bg-white shadow">
            <p className="text-gray-600">
                📦 Box Details — Grid View (placeholder)
            </p>
        </div>
    );
};

export default BoxDetailsGridView;
